1: To enable Management site, run: 
    INSERT INTO `Backend`.`web_enabled` (`siteEnabled`, `websiteId`) VALUES (1, 'management-panel');

2. TO enable Public website, run:
    INSERT INTO `Backend`.`web_enabled` (`siteEnabled`, `websiteId`) VALUES (1, 'public-site');

